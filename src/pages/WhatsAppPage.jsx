import{Webhook,MessagesSquare,Play,Building2}from"lucide-react";
import{Badge}from"../components/common/Badge";
import{SECTORS}from"../data/sectors";
import{whatsappConnection,whatsappWindow,whatsappTemplates,whatsappCost,whatsappAutomations}from"../data/whatsappMock";

export function WhatsAppPage(){
return <div className="page"><div className="page-head"><div><h2>WhatsApp oficial</h2><p>Estrutura visual para Cloud API, templates, custos e automações.</p></div><Badge tone="success">Conectado</Badge></div><div className="wh-grid"><div className="panel"><h3>Conexão</h3><label>Número<input defaultValue={whatsappConnection.number}/></label><label>WABA<input defaultValue={whatsappConnection.waba}/></label><label>Webhook<input defaultValue={whatsappConnection.webhook}/></label><button className="primary"><Webhook/>Testar webhook</button></div><div className="panel"><h3>Janela de 24h</h3><div className="big-number">{whatsappWindow.duration}</div><small>janela ativa do atendimento selecionado</small><div className="line"><span>Templates disponíveis</span><b>{whatsappWindow.templatesAvailable}</b></div><div className="line"><span>Mensagens este mês</span><b>{whatsappWindow.messagesThisMonth}</b></div></div><div className="panel"><h3>Templates</h3>{whatsappTemplates.map(t=><div className="template" key={t}><MessagesSquare/><span>{t}</span><button><Play/></button></div>)}</div><div className="panel"><h3>Custo estimado</h3><div className="big-number">{whatsappCost.total}</div><small>simulação visual mensal</small><div className="line"><span>Serviço</span><b>{whatsappCost.service}</b></div><div className="line"><span>Utilidade</span><b>{whatsappCost.utility}</b></div><div className="line"><span>Marketing</span><b>{whatsappCost.marketing}</b></div></div><div className="panel"><h3>Automações</h3>{whatsappAutomations.map(a=><label className="switch" key={a.name}><span>{a.name}</span><input type="checkbox" defaultChecked={a.enabled}/></label>)}</div><div className="panel"><h3>Setores</h3>{SECTORS.map(x=><div className="line" key={x}><Building2/><span>{x}</span><b>Ativo</b></div>)}</div></div></div>
}
